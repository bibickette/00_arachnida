créer un venv : `python3 -m venv .venv`
lancer mon env python `source .venv/bin/activate`
    et quitter : `deactivate`

installer des packages : activate et `pip install package_name` 
lister les packages installés : `pip list`
mettre a jour : `pip install --upgrade package_name`
uninstall : `pip uninstall package_name`
voir les dependances dun packet : `pip show package_name` 

exporter les dependances dans un requirements.txt (cest comme le package.json) : `pip freeze > requirements.txt`
installer les dependances depuis un requirements.txt : `pip install -r requirements.txt`

verifier les mise a jours de secu : `pip list --outdated`

Ajouter le shebang (comme mentionné ci-dessus). #!/usr/bin/env python3
Changer les permissions du fichier pour le rendre exécutable à l’aide de la commande chmod :
Fenêtre de terminal
chmod +x mon_script.py

Exécuter le script directement depuis le terminal :
Fenêtre de terminal
./mon_script.py
ou
python3 mon_script.py

Ouvrir le python interactif : dans le .venv activé, taper python, la on peut ecrire du code




Script : fichier texte interprété par un langage (Python, Bash, etc.)
Binaire : programme compilé en code machine (C, C++, Go…)



from dataclasses import dataclass # permet des creer des classes

@dataclass
class Args:
    flag_r: bool = False
    depth: int = 5
    url: str | None = None
    path: str | None = None


from src.arg_checker import arg_check ; pour import


    args = Args()





    FIFO veut dire “First In, First Out” = “Premier entré, premier sorti”.

Imagine une file d’attente :

la première personne qui arrive est la première servie.
En programmation, une structure FIFO (une queue) fonctionne pareil :

tu ajoutes les éléments à la fin
tu retire/traites les éléments au début

BFS (Breadth‑First Search) = “je fais niveau par niveau”
Tu visites d’abord toutes les pages à profondeur 0, puis profondeur 1, puis profondeur 2…

DFS (Depth‑First Search) = “je vais au fond d’abord”
Tu suis un lien, puis un lien de ce lien, etc. le plus profond possible, puis tu reviens en arrière.
Structure typique : pile (stack) ou récursion.




Copilot said: `requests.Session()` est un objet de la librairie
requests.Session() est un objet de la librairie requests qui te permet de faire plusieurs requêtes HTTP en réutilisant la même “session” au lieu d’appeler requests.get(...) à chaque fois.

Pourquoi c’est utile ?
Quand tu fais beaucoup de GET (scraper), une Session :

réutilise les connexions TCP (keep-alive) → moins de “handshake” → plus rapide
garde des infos communes (headers, cookies, auth) → tu n’as pas à les repasser partout
a souvent un comportement plus “propre” pour un crawler (tout passe par le même client)